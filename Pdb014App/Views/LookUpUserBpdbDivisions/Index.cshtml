@model IEnumerable<Pdb014App.Models.UserManage.LookUpUserBpdbDivision>

@{
    ViewData["Title"] = "Index";
}

<h1>BPDB Division List</h1>

<p>
    <a asp-action="Create">Add New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BpdbDivisionName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BpdbDivisionName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BpdbDivisionId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BpdbDivisionId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BpdbDivisionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
