@model IEnumerable<Pdb014App.Models.PDB.InsulatorModels.LookUpInsulatorPinAndPostType>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>List of Insulator Pin/Post Type</h2>

<p>
    <a asp-action="Create">Add New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.InsulatorPinAndPostTypeName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.InsulatorPinAndPostTypeName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.InsulatorPinAndPostTypeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.InsulatorPinAndPostTypeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.InsulatorPinAndPostTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
