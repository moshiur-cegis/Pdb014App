@model IEnumerable<Pdb014App.Models.PDB.ServicePointModels.LookUpBusinessType>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

    <h2>List of Business Type</h2>

<p>
    <a asp-action="Create">Add New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BusinessTypeName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BusinessTypeName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BusinessTypeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BusinessTypeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BusinessTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
