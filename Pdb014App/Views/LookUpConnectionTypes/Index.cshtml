@model IEnumerable<Pdb014App.Models.PDB.ServicePointModels.LookUpConnectionType>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

    <h2>List of Connection Type</h2>

<p>
    <a asp-action="Create">Add New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ConnectionTypeName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ConnectionTypeName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ConnectionTypeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ConnectionTypeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ConnectionTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
