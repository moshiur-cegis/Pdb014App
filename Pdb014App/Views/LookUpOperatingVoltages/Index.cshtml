@model IEnumerable<Pdb014App.Models.PDB.ServicePointModels.LookUpOperatingVoltage>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

    <h2>List of Operating Voltage Type</h2>

<p>
    <a asp-action="Create">Add New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OperatingVoltageName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OperatingVoltageName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.OperatingVoltageId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.OperatingVoltageId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.OperatingVoltageId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
