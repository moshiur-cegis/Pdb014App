@model IEnumerable<Pdb014App.Models.PDB.PoleModels.TblPolePicture>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PictureName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PictureLocation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PolePictureToPole)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PictureName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PictureLocation)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PolePictureToPole.PoleId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a>
                @*<a asp-action="Details" asp-route-id="@item.PolePictureId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PolePictureId">Delete</a>*@
            </td>
        </tr>
}
    </tbody>
</table>Id
