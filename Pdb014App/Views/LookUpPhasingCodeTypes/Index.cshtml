@model IEnumerable<Pdb014App.Models.PDB.ServicePointModels.LookUpPhasingCodeType>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

    <h2>List of Phasing Code Type</h2>

<p>
    <a asp-action="Create">Add New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PhasingCodeName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PhasingCodeName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PhasingCodeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PhasingCodeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PhasingCodeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
