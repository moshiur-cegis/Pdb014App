@model IEnumerable<Pdb014App.Models.UserManage.LookUpUserContentType>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ContentTypeName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ContentTypeName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ContentTypeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ContentTypeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ContentTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
