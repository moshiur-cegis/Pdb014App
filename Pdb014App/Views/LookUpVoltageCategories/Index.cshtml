@model IEnumerable<Pdb014App.Models.PDB.ServicePointModels.LookUpVoltageCategory>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

    <h2>List of Voltage Category</h2>

<p>
    <a asp-action="Create">Add New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.VoltageCategoryName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.VoltageCategoryName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.VoltageCategoryId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.VoltageCategoryId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.VoltageCategoryId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
